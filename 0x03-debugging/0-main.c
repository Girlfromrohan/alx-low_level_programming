#include "main.h"

/**
 * main - entry point
 * description: testing function that prints if int is negative or positive
 * Return: always 0 (success)
 */

int main(void)
{
	int i = 0;

	positive_or_negative(i);
	return (0);
}
